import { gsap } from gsap;

//animation for the 'mobile first' SVG in the include carousel
//returns a gsap timeline object that holds the animation of the svg
export default function mobileAniInit() {
    var tl = gsap.timeline();

        //yada yada yada

    return tl
}